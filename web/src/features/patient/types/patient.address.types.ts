export interface AddressResponse {
    addressLine: string;
    city: string;
    state: string;
    pinCode: string;
    country: string;
}

export interface AddressRequest {
    addressLine: string;
    city: string;
    state: string;
    pinCode: string;
    country: string;
}
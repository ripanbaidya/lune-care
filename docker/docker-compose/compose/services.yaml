# Spring profile strategy: SPRING_PROFILES_ACTIVE=dev,docker
#
# Load order (last wins):
#   application.yml
#   → application-dev.yml        (localhost refs, dev behaviour)
#   → application-docker.yml     (overrides localhost → docker service names)
#   → services/<name>/application.yml
#   → services/<name>/application-dev.yml
#   → services/<name>/application-docker.yml
#
# Boot order: infra → databases → config-server → eureka → services → api-gateway
#
# To enable OpenTelemetry tracing on a service, add:
#   extends:
#     file: commons.yaml
#     service: open_telemetry_environment_config

services:

  # Infrastructure

  # Config Server — must start first; all services pull their config from here
  config-server:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-config-server:${IMAGE_TAG:-latest}
    container_name: lunecare-config-server
    ports:
      - "8888:8888"
    environment:
      SPRING_APPLICATION_NAME: config-server
      SPRING_PROFILES_ACTIVE: docker
      SPRING_CLOUD_CONFIG_SERVER_GIT_URI: ${GIT_URI}
      ENCRYPT_KEY: ${ENCRYPT_KEY}
    networks:
      - lunecare_network
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:8888/actuator/health/readiness | grep -q UP || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  # Eureka Server — service registry; depends on config-server
  eureka-server:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-eureka-server:${IMAGE_TAG:-latest}
    container_name: lunecare-eureka-server
    ports:
      - "8761:8761"
    environment:
      SPRING_APPLICATION_NAME: eureka-server
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-dev,docker}
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
    networks:
      - lunecare_network
    healthcheck:
      test: [ "CMD-SHELL", "curl -sf http://localhost:8761/actuator/health/readiness | grep -q UP || exit 1" ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 40s

  # Services

  # Auth Service — port 8081
  # Storage: PostgreSQL (schema: auth) + Redis (token blacklist)
  # Requires RSA private + public key mounts for JWT signing
  auth:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-auth:${IMAGE_TAG:-latest}
    container_name: lunecare-auth
    ports:
      - "8081:8081"
    environment:
      SPRING_APPLICATION_NAME: auth
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    volumes:
      - ../../../secrets/keys:/app/secrets/keys:ro
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Patient Service — port 8082
  # Storage: PostgreSQL (schema: patient)
  patient:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-patient:${IMAGE_TAG:-latest}
    container_name: lunecare-patient
    ports:
      - "8082:8082"
    environment:
      SPRING_APPLICATION_NAME: patient
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Doctor Service — port 8083
  # Storage: PostgreSQL (schema: doctor)
  doctor:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-doctor:${IMAGE_TAG:-latest}
    container_name: lunecare-doctor
    ports:
      - "8083:8083"
    environment:
      SPRING_APPLICATION_NAME: doctor
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Appointment Service — port 8084
  # Storage: PostgreSQL (schema: appointment) + Redis (cache)
  appointment:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-appointment:${IMAGE_TAG:-latest}
    container_name: lunecare-appointment
    ports:
      - "8084:8084"
    environment:
      SPRING_APPLICATION_NAME: appointment
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Payment Service — port 8085
  # Storage: PostgreSQL (schema: payment)
  payment:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-payment:${IMAGE_TAG:-latest}
    container_name: lunecare-payment
    ports:
      - "8085:8085"
    environment:
      SPRING_APPLICATION_NAME: payment
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Notification Service — port 8086
  # Storage: MongoDB (notification_db)
  notification:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-notification:${IMAGE_TAG:-latest}
    container_name: lunecare-notification
    ports:
      - "8086:8086"
    environment:
      SPRING_APPLICATION_NAME: notification
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Feedback Service — port 8087
  # Storage: MongoDB (feedback_db)
  feedback:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-feedback:${IMAGE_TAG:-latest}
    container_name: lunecare-feedback
    ports:
      - "8087:8087"
    environment:
      SPRING_APPLICATION_NAME: feedback
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Admin Service — port 8088
  # No dedicated DB; aggregates via Feign (doctor-service, auth-service)
  admin:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-admin:${IMAGE_TAG:-latest}
    container_name: lunecare-admin
    ports:
      - "8088:8088"
    environment:
      SPRING_APPLICATION_NAME: admin
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

  # Gateway

  # API Gateway — port 8080
  # Entry point for all external traffic.
  # Requires RSA public key mount for JWT verification.
  # Start last — after all domain services are registered in Eureka.
  api-gateway:
    image: ${DOCKERHUB_ACCOUNT:-ripanbaidya}/lunecare-api-gateway:${IMAGE_TAG:-latest}
    container_name: lunecare-api-gateway
    ports:
      - "8080:8080"
    environment:
      SPRING_APPLICATION_NAME: api-gateway
      SPRING_PROFILES_ACTIVE: dev,docker
      SPRING_CONFIG_IMPORT: configserver:${CONFIG_SERVER_URL}
    volumes:
      - ../../../secrets/keys:/app/secrets/keys:ro
    depends_on:
      config-server:
        condition: service_healthy
      eureka-server:
        condition: service_healthy
    networks:
      - lunecare_network

networks:
  lunecare_network:
    name: lunecare_network
    driver: bridge